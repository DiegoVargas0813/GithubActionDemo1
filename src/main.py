#import os # Codigo deliberadamente erroneo con un import no utilizado e implementacion incorrecta de la calculadora para testeo.

class Calculator:
    def sum(self, a: int, b:int) -> int:
        return a + b